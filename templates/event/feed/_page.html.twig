<turbo-frame id="event-feed-page-{{ before|default(nextCursor) }}">
    {% include 'event/feed/_posts_list.html.twig' with {
        event: event,
        posts: posts,
        hypedPostIds: hypedPostIds,
        nextCursor: nextCursor,
    } only %}
</turbo-frame>
